package com.asaf.whatnext.enums;

/**
 * Enum representing the different sources of events.
 * Used to indicate which provider the event comes from.
 */
public enum EventSourceType {
    BILETIX,
    PASSO,
    MOBILET,
    MANUAL
}