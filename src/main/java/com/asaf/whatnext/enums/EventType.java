package com.asaf.whatnext.enums;

/**
 * Enum representing the different types of events.
 * Used to replace string literals in the code.
 */
public enum EventType {
    CONCERT,
    THEATER,
    STANDUP,
    EXHIBITION
}